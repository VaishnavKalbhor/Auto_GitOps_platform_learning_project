# Observability

*(To be filled in with real screenshots once kube-prometheus-stack is installed — see Day 10 in the learning log.)*

## What's being monitored

- Cluster CPU/memory usage
- Pod status and restarts
- Deployment replica counts (ties into the HPA test)
- Node readiness
- HPA scaling events
